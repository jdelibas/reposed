'use strict';

describe('module rp.repo', function () {

    it('should exist', function () {
        // Arrange
        // Act
        // Assert
        expect(angular.module('rp.repo')).toBeDefined();
    });

});
